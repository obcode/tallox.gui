name: docker

on:
  release:
    types: [published]

permissions:
  contents: read
  packages: write

jobs:
  build-and-push:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v7

      - uses: docker/login-action@v4.6.0
        with:
          registry: ghcr.io
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}

      - id: meta
        uses: docker/metadata-action@v6
        with:
          images: ghcr.io/${{ github.repository }}

      - uses: docker/build-push-action@v7
        with:
          context: .
          push: true
          tags: ${{ steps.meta.outputs.tags }}
          labels: ${{ steps.meta.outputs.labels }}
          build-args: |
            APP_VERSION=${{ github.event.release.tag_name }}

  dispatch-deploy:
    needs: build-and-push
    # Feature-Gate wie beim Release und beim Deploy: ohne DISPATCH_TOKEN bricht `gh api` mit
    # leerem GH_TOKEN ab und färbt den Release-Lauf rot. Zusammen mit AUTO_DEPLOY in
    # tallox.dev scharfschalten, sobald der Host steht.
    if: ${{ vars.DEPLOY_DISPATCH_ENABLED == 'true' }}
    runs-on: ubuntu-latest
    steps:
      # Die Deploy-Artefakte liegen im privaten Repo tallox.dev. DISPATCH_TOKEN ist ein
      # fine-grained PAT mit ausschließlich "contents: write" auf obcode/tallox.dev.
      - name: Deploy in tallox.dev auslösen
        env:
          GH_TOKEN: ${{ secrets.DISPATCH_TOKEN }}
        run: |
          set -euo pipefail
          gh api repos/obcode/tallox.dev/dispatches \
            -f event_type=deploy-gui \
            -F "client_payload[tag]=${{ github.event.release.tag_name }}"
